# Recommendations for HSR

> Auto-generated by Hermes Agent  
> Created: 2026-05-06T08:58:33.363608  
> Legend: `[ ]` pending, `[~]` in progress, `[x]` done, `[-]` dismissed

## Code Quality

<!-- Suggestions: type hints, docstrings, error handling, tests, refactoring -->

[x] **FLYVBJERG_DEMAND_FACTOR=0.486 verified correct** — equals 48.6% of forecast = (1.0 − 51.4%) shortfall per Flyvbjerg 2005. The earlier belief that 0.514 was correct confused shortfall% with factor. Code is correct as-is; only documentation fixed.

[ ] Add unit tests in a separate tests/ directory. The only test coverage is regression_test() at line 934 which does coarse range checks (BCR between 0 and 3, NPV finite). Add targeted unit tests for run_cba() edge cases: zero passengers, 100% tunnel share, negative VOT, discount rate = 0%, appraisal_yrs = 1, construction_years = 0. Use pytest with fixtures for the CZ country profile + VRT Praha–Brno corridor as the baseline pair.

[ ] Remove hardcoded absolute path in regression_test() docstring at line 936. It references `/home/tomaspaleta/Documents/GitHub/HSR` which will fail on any other machine. Replace with `os.path.dirname(__file__)` or document how to run it with `python -m pytest`.

[ ] Add Python type hints to the five core calculation functions. run_cba() returns a tuple of (dict, DataFrame) but has no annotation; compute_capex(), threshold_search(), run_monte_carlo(), and build_inputs() also lack type hints. This catches bugs early (mypy) and serves as living documentation for the ~40-key parameter dict each function expects.

[ ] Validate inputs in build_inputs() before they reach run_cba(). Currently physically impossible values (negative line length, tunnel+viaduct > 100%, access_egress_hsr > current_time) pass through silently and produce NaN or nonsensical NPV. Add a validation layer that raises clear error messages for each illegal input combination.

## Structure & Organization

<!-- Suggestions: file splitting, naming, modularity -->

[ ] Extract COUNTRY_PRESETS (lines 139–201, ~63 lines) into data/country_profiles.json. Hardcoded in the middle of a 2202-line monolith, these profiles are configuration data, not logic. Moving to JSON lets non-programmers update VOT, discount rates, and CO₂ prices without touching Python. Load at module init with json.load().

[ ] Extract CORRIDOR_PRESETS (lines 206–222, ~17 lines compacted into dense one-liners) into data/corridor_presets.json. Same rationale as country profiles. While you are at it, expand each preset from a single-line dict literal to a readable multi-line object so reviewers can verify the 50+ fields per corridor.

[ ] Split hsr_cba_app_upg2.py into three modules: cba_engine.py (pure functions: run_cba, compute_capex, threshold_search, run_monte_carlo, _irr_manual, generate_documentation), presets.py (COUNTRY_PRESETS loader + CORRIDOR_PRESETS loader from JSON), and app.py (Streamlit UI only). The current file mixes business logic with presentation; separating them makes the engine importable/testable without starting Streamlit.

[ ] Delete or archive backup directories backup_2026-04-26/ and backup_2026-04-27/. These contain old versions (v1, upg1, pre-citation-fix upg2) that are already tracked in git history. Keeping duplicate copies in the working tree adds 3 stale Python files (~5000 lines total) that confuse contributors about which version is canonical.

[ ] Rename hsr_cba_app_upg2.py → app.py (after splitting, see above). The "upg2" suffix implies this is an interim revision, undermining confidence that this is the stable release. Also remove the obsolete hsr_cba_app.py (648 lines, original v1) and hsr_cba_app_upg1.py — both superseded by upg2.

[ ] Add a .gitignore file. The repo currently lacks one entirely. Minimum entries: __pycache__/, *.pyc, .streamlit/secrets.toml, .venv/, .DS_Store, and any future API key files. The __pycache__ directory is currently visible in the working tree.

## Documentation

<!-- Suggestions: README improvements, inline docs, examples -->

[ ] Write a README.md with a quickstart section. Current repo has no README. Include: (a) pip install -r requirements.txt, (b) streamlit run app.py, (c) select Czech Republic + VRT Praha–Brno from sidebar dropdowns for a worked example, (d) screenshot of expected output. Without this, new users reverse-engineer usage from the source code.

[ ] Document the data provenance of each corridor preset inline in corridor_presets.json. For example, TGV Paris–Lyon values should cite "SNCF bilan LOTI 1984"; HS2 Phase 1 should cite "HS2 Ltd Economic Case 2023, Table 4.2"; VRT Praha–Brno should cite source assumptions. Currently all presets are bare numbers with no audit trail — problematic for a tool marketed as academically rigorous.

[ ] Add docstrings with parameter descriptions to every public function. run_cba() at line 300 accepts a ~40-key dict called `p` with no documented schema. A developer reading the code must trace through the entire function body to discover what keys are consumed. Document the full parameter dict schema in the docstring.

[ ] Move the "Main limitations" block (module docstring lines 31–38) into a standalone LIMITATIONS.md file referenced from both README.md and technical_paper.md. This makes limitations discoverable without opening a 2202-line source file and signals methodological honesty to reviewers.

[ ] Add a citation provenance comment on every COUNTRY_PRESET entry. Example for France: `# CGEDD (2020), "Valeur du temps", Table 1; Quinet (2013) discount rate`. Currently the 10 country profiles are bare dicts — readers cannot verify whether Czech VOT values come from MD ČR Rezortní metodika or HEATCO defaults.

## Performance & Security

<!-- Suggestions: bottlenecks, dependencies, secrets handling -->

[ ] Cache the Monte Carlo simulation with @st.cache_data. On every Streamlit widget change the entire Monte Carlo panel reruns (line 599, default n=5000 iterations × 40 years of cash flows). For complex corridors the UI blocks for seconds. Wrap run_monte_carlo() with @st.cache_data keyed on (frozenset(p.items()), mc_config_keys, n) so identical inputs return instantly.

[ ] Pin dependency versions exactly in requirements.txt. Currently streamlit>=1.32.0 uses floating >= specifiers. A future Streamlit 2.x breaking change could silently break the app. Lock to known-good versions: streamlit==1.32.0, plotly==5.18.0, pandas==2.0.0, numpy==1.24.0. Better yet, add a pip freeze > requirements.lock.

[ ] Add input bounds warnings in the Streamlit sidebar. When a user sets VOT outside the typical CEE range (business VOT below €15 or above €80/hr), display a st.warning() with the expected range from national guidelines. Similarly flag when tunnel share + viaduct share exceeds 100%. This prevents nonsensical inputs that produce misleading NPV figures — especially important for students and policy makers unfamiliar with rail engineering norms.

[ ] Replace the regression test baseline with a snapshot file (tests/baseline_cz_vrt.json). Currently regression_test() at line 934 recomputes live and does coarse range checks (BCR between 0 and 3). If a code change accidentally shifts BCR from 1.8 to 0.9, it still passes. A snapshot test catches drift: store S dict for Praha–Brno + Czech profile as JSON, compare with a tolerance of ±1% in CI.

---

*Last updated: 2026-05-06T09:20:00*